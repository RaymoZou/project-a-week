# Week 26 of Paw - SDL Sprite Animator

An simple application for animating spritesheets.

## How to use:
1. Install dependencies `SDL2` and `SDL2_image`
2. Compile with `make`
3. Run with `./main.exe spritesheet.png`
